import { Box } from '@mui/material';
import React from 'react';
import { useChatContext } from '../../context';
import { IMessageContent } from '../../types';

export function MessageFooterComponent(props: { message: IMessageContent }): JSX.Element | null {
  return null; // No footer registry in Prime Agent
}
