import React, { useEffect, useState } from 'react';
import { IMessage, IMessageContent } from '../../types';

export function MessagePreambleComponent(props: { message: IMessage }): JSX.Element | null {
  return null; // No preamble registry in Prime Agent
}
